[package]
name = "zapdrop-companion"
version = "0.1.0"
edition = "2021"
rust-version = "1.77.2"
description = "Headless Zapdrop protocol companion for legacy and minimal clients"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
