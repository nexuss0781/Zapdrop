{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Scoped Zapdrop desktop capabilities for local file selection and transfer.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
